# CRDs are installed/retained by Pulumi on bootstrap and must not be managed by ArgoCD.
# SSA on 1MB CRDs causes "metadata.annotations: Too long" — ArgoCD must not touch them.
# CRD upgrades must be applied manually (kubectl apply --server-side -f) when ESO major versions change.
installCRDs: false
