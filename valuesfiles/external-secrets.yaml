installCRDs: true

certController:
  enabled: false

webhook:
  certManager:
    enabled: true
    cert:
      issuerRef:
        name: external-secrets-selfsigned
        kind: ClusterIssuer
        group: cert-manager.io
