image:
  tag: v2.0.6
service:
  type: ClusterIP
ingress:
  enabled: true
  className: tailscale
  hosts:
    - paths:
      - path: /
        pathType: ImplementationSpecific
  tls:
    - hosts:
      - openspeedtest

