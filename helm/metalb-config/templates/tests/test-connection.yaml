apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metalb-config.fullname" . }}-test-connection"
  labels:
    {{- include "metalb-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metalb-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
